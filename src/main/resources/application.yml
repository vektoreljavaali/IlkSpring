server:
  port: 8080

muhammeturls:
  urun:
    save: ${urun_save}
    update: ${urun_update}
    delete: "/delete"
    findall: "/findall"
  musteri:
    save: "/save"



spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Eticaret
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      format_sql: true